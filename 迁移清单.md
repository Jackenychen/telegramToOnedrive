# Telegram → OneDrive 服务 · 打包迁移清单

> 适用：从一台 VPS **打包拷贝**到另一台，保留登录态与处理进度。  
> 部署目录示例：`/root/telegram`（以下按此路径写，请按实际修改）。

---

## 一、迁移前（旧机器）

### 1. 确认信息

| 项 | 旧机值（请填空备份） |
|----|----------------------|
| 项目目录 | `/root/telegram` |
| systemd 服务名 | `tg-onedrive` |
| 监听 `source` | 见 `config.ini` |
| rclone `remote` 名 | 如 `onedrive` |
| OneDrive 目录 `dest_dir` | 如 `Telegram` |
| 91 是否同机 | 是 / 否 |

### 2. 停服务（避免 session / processed 写入半截）

```bash
sudo systemctl stop tg-onedrive
systemctl status tg-onedrive    # 应为 inactive
```

### 3. 可选：记录最近处理进度

```bash
tail -c 200 /root/telegram/processed.json
journalctl -u tg-onedrive -n 5 --no-pager
```

---

## 二、必须打包的文件

### A. 项目目录（核心）

在旧机执行（**先已 stop 服务**）：

```bash
cd /root
tar czf telegram-migrate.tar.gz \
  --exclude='telegram/tmp/*' \
  --exclude='telegram/__pycache__' \
  --exclude='telegram/*.session-journal' \
  telegram/
```

**包内务必包含：**

| 文件/目录 | 必需 | 说明 |
|-----------|------|------|
| `config.ini` | ✅ | API、群 ID、rclone、连接数；含密钥，`chmod 600` |
| `tg_saved.session` | ✅ | Telegram 登录态；无则新机要重新验证码 |
| `processed.json` | ✅ | 已处理 message id；无则可能重复下或需冷启动回扫 |
| `tg_to_onedrive.py` 等 `.py` | ✅ | 主程序（若只拷 tar 整目录已含） |
| `venv/` | 建议 | 同 Linux x86_64 可直接用；跨架构见下文重建 |
| `tg-onedrive.service` | 建议 | 需改新机路径 |
| `requirements.txt` | 建议 | 重建 venv 时用 |
| `tmp/` | ❌ | 不必；空目录即可 |

### B. rclone 配置（**极易漏拷**）

OneDrive 授权在 rclone 配置里，**不在**项目目录：

```bash
tar czf rclone-config.tar.gz -C /root .config/rclone
# 若用其他用户跑 rclone，改为该用户的 ~/.config/rclone
```

新机解压到对应用户家目录：`~/.config/rclone/rclone.conf`。

验证旧机授权仍有效：

```bash
rclone lsd onedrive:
rclone lsl onedrive:Telegram/ | head
```

---

## 三、传到新机器

```bash
scp telegram-migrate.tar.gz rclone-config.tar.gz 用户@新IP:/root/
# 或用 rsync、面板下载等
```

---

## 四、新机器环境准备

### 1. 系统依赖

```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip curl
curl https://rclone.org/install.sh | sudo bash
rclone version
```

### 2. 解压

```bash
cd /root
tar xzf telegram-migrate.tar.gz          # 得到 /root/telegram
mkdir -p ~/.config
tar xzf rclone-config.tar.gz -C ~      # 得到 ~/.config/rclone
chmod 600 /root/telegram/config.ini
chmod 600 /root/telegram/tg_saved.session
```

### 3. Python 虚拟环境

**同架构（如都是 amd64）且 tar 里已有 `venv/`：**

```bash
/root/telegram/venv/bin/python -c "import telethon; print('OK')"
```

若报错或跨架构 / 跨发行版，重建：

```bash
cd /root/telegram
rm -rf venv
python3 -m venv venv
venv/bin/pip install -r requirements.txt
```

### 4. 验证 rclone

```bash
rclone lsd onedrive:
# 若 token 失效：
# rclone config reconnect onedrive:
# （tenant 提示留空）
```

> OneDrive 账号须与 **91 项目**使用的是同一个。

---

## 五、systemd 服务

编辑 `tg-onedrive.service`，确认路径为新机实际路径：

```ini
WorkingDirectory=/root/telegram
ExecStart=/root/telegram/venv/bin/python /root/telegram/tg_to_onedrive.py
```

安装并启动：

```bash
sudo cp /root/telegram/tg-onedrive.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable tg-onedrive
sudo systemctl start tg-onedrive
```

---

## 六、启动后检查（新机）

```bash
systemctl status tg-onedrive
journalctl -u tg-onedrive -n 20 --no-pager
```

**正常日志应类似：**

```text
已登录：xxx (id=..., Premium=True)
增量补漏：拉取 id > xxxx 的未处理消息…
增量补漏完成，入队 N 条视频
开始监听 source=...，等待新视频…
```

| 检查项 | 命令 |
|--------|------|
| 服务在跑 | `systemctl is-active tg-onedrive` → `active` |
| 无报错 | `journalctl -u tg-onedrive` 无 Traceback / 处理失败 |
| 临时目录空 | `ls /root/telegram/tmp/` 基本为空 |
| processed 在 | `wc -c /root/telegram/processed.json` |

往群里**转发 1 个小视频**测试：

```bash
journalctl -u tg-onedrive -f
# 应出现：下载 → rclone 上传 → 完成 id=
```

---

## 七、旧机器收尾（确认新机正常后）

```bash
# 旧机可选：停服务并禁用，避免双实例同时登录同一 TG 账号
sudo systemctl stop tg-onedrive
sudo systemctl disable tg-onedrive
```

⚠️ **同一 `tg_saved.session` 不宜两台同时长期在线**，可能踢线或异常。

---

## 八、常见问题

| 现象 | 处理 |
|------|------|
| 启动要重新登录 | `*.session` 未拷全或损坏；重新 `venv/bin/python tg_to_onedrive.py` 交互登录 |
| `rclone 失败` | 未拷 `~/.config/rclone` 或 token 过期 → `rclone config reconnect` |
| 重复下载很多视频 | `processed.json` 未拷；临时加大 `backfill_limit` 或接受去重上传改名 |
| `venv` 报 ImportError | 新机重建 venv + `pip install -r requirements.txt` |
| 群收不到消息 | `config.ini` 里 `source` 是否仍为正确群 ID（超级群升级会变 `-100` 开头） |
| 91 扫不到 | `dest_dir` 是否在 91 的 OneDrive `rootID` 下；是否同一账号 |

---

## 九、最小文件清单（打印勾选）

```
[ ] systemctl stop tg-onedrive（旧机）
[ ] telegram-migrate.tar.gz（含 config.ini / session / processed / venv / 代码）
[ ] rclone-config.tar.gz（~/.config/rclone）
[ ] 新机：python3、rclone 已安装
[ ] 新机：解压 + chmod 600 敏感文件
[ ] 新机：venv 可用或已重建
[ ] 新机：rclone lsd onedrive: 成功
[ ] 新机：tg-onedrive.service 路径已改
[ ] 新机：systemctl start 且日志「已登录」「开始监听」
[ ] 新机：转发 1 条视频测试「完成 id=」
[ ] 旧机：已停服务（避免双开）
```

---

## 十、与 GitHub 的关系

- **源码**也可在新机 `git clone https://github.com/Jackenychen/telegramToOnedrive`，再**只从旧机覆盖**：
  - `config.ini`
  - `tg_saved.session`
  - `processed.json`
- 打包方式适合 **不改 Git、整目录原样搬迁**。

---

*文档随项目更新；敏感文件切勿上传到公开仓库。*