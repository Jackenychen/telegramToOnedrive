# P1 优化修改方案

> 基于 2026-06-03 讨论整理。实施前可先审阅本文；代码改动按文末「实施顺序」分步提交。

---

## 目标与范围

### 目标

| 编号 | 优化项 | 要解决的问题 |
|------|--------|----------------|
| P1-1 | 并行下载连接池 + 空闲回收 | 每个大文件重复建/拆 8 条 TG 连接，浪费 1~10s/条 |
| P1-2 | `remote_exists` 内存缓存 | 每条视频上传前多次 `rclone lsf` 问 OneDrive，API 慢 |
| P1-3 | rclone 异步子进程 | `subprocess.run` 阻塞 asyncio 事件循环 |

### 不在 P1 范围（留给 P2）

- 下载/上传流水线（边传边下一条）
- 失败自动重试队列
- `processed.json` 增量存储改造
- 启动时强制全量 `rclone lsf` 落盘索引文件

---

## 总体原则

1. **行为不变**：同名不覆盖、并行回退单连接、FloodWait 重入队、增量补漏逻辑均保持。
2. **可配置**：空闲回收、缓存预热用 `config.ini` 开关，默认保守。
3. **可回退**：每项可独立关闭；出问题改配置或 revert commit 即可。
4. **不引入本地 OneDrive 全量镜像**：P1-2 仅进程内 `set`，可选启动预热，**不要求**事先拉一份目录到磁盘。

---

## P1-1：并行连接池（SenderPool）

### 现状

- `fast_download._run_parallel()` 每次调用 `_make_senders()`，结束后 `disconnect` 全部连接。
- 8 连接串行建立，日志可见大量 Connecting/Disconnecting。

### 方案

在 `fast_download.py` 增加模块级或客户端绑定的 `SenderPool`：

```text
acquire(client, dc_id, connections)
  → 若池中有 (dc_id, connections) 且连接仍可用 → 直接返回
  → 否则 _make_senders() 填入池

release(dc_id, connections)
  → 不 disconnect，更新 last_used_at

discard(dc_id, connections)
  → disconnect 并清空该 key（CDN/建连失败/FileReference 刷新后整轮失败时）

空闲回收（每次 release 或定时检查）：
  若 now - last_used_at > sender_pool_idle_sec → discard 该池
```

### 配置项（`config.ini` / `config.example.ini`）

```ini
[download]
# 并行 sender 池空闲多少秒后关闭；0 = 不自动关闭（常驻）
sender_pool_idle_sec = 60
```

### 与 CPU / 资源

- 空闲连接：**几乎不占 CPU**，占少量内存与 socket。
- `sender_pool_idle_sec = 60`：1 分钟无并行下载则关池；下一条大文件多花建连时间（约 1~3s），换更少僵死连接。

### 关键实现细节

| 点 | 做法 |
|----|------|
| 池 key | `(dc_id or session.dc_id, connections)` |
| 建连 | 仍**串行** `_make_senders`，禁止并发导出鉴权 |
| 失效 | `_NeedFallback`、任意 worker 异常、communicate 前检测到 disconnect → `discard` |
| 进程退出 | `atexit` 或 `main()` 收尾 `pool.close_all()` |
| 小文件 | `< MIN_PARALLEL_SIZE` 不走池，仍 `download_media` 单连接 |

### 修改文件

- `fast_download.py`：Pool 类、`_run_parallel` 改用池
- `tg_to_onedrive.py`：`main()` 退出时关闭池（可选传入 pool 实例）
- `config.example.ini`：新增 `sender_pool_idle_sec`

### 验收

- 连续下载 2 个大文件：第二次日志**无** 8 次 Connecting（池命中）。
- 空闲 >60s 后再下大文件：日志出现建连，随后正常完成。
- `bench_dl.py` 测速与改前对比，sha256 仍一致。

---

## P1-2：`remote_exists` 内存缓存

### 现状

- `resolve_unique_name()` 最多 3+ 次同步 `rclone lsf`。
- `processed.json` 不记录云端文件名。

### 方案

在 `tg_to_onedrive.py` 维护进程内集合（**不落盘**）：

```python
_REMOTE_EXISTS: set[str] = set()   # 已确认在 OneDrive 存在的文件名（含路径相对名）
```

| 时机 | 操作 |
|------|------|
| `remote_exists(name)` 且 `name in _REMOTE_EXISTS` | 直接返回 `True`，**不**调 rclone |
| `remote_exists(name)` 未命中 | `await`/`to_thread` 执行 `rclone lsf`；若存在则 `add(name)` |
| `rclone moveto` 成功 | `_REMOTE_EXISTS.add(最终文件名)` |
| 可选：启动预热 | 见下 |

**未命中时行为与现在完全一致**，不会误判覆盖。

### 可选预热（默认关闭）

```ini
[download]
# 启动时是否 rclone lsf 列出 dest_dir 预热缓存（目录很大时启动慢）
warm_remote_cache = false
```

- `false`（默认）：缓存从空开始，运行中积累；**不需要**事先从 OneDrive 拉文件到本机磁盘。
- `true`：启动时执行一次 `rclone lsf REMOTE:DEST_DIR/ --files-only`，将文件名批量 `update` 进 set。

### 边界

| 场景 | 行为 |
|------|------|
| 人工在 OneDrive 删文件 | 缓存仍以为存在 → 可能多用改名后缀；极少见，重启进程清空缓存 |
| 人工上传同名文件 | 未在缓存 → 第一次 `lsf` 仍能发现 |
| 删 `processed.json` 冷启动 | 与缓存无关；仍靠 `lsf` + 改名 |

### 修改文件

- `tg_to_onedrive.py`：`remote_exists`、`resolve_unique_name`、`transfer` 成功后登记
- `config.example.ini`：`warm_remote_cache`

### 验收

- 第二条同名逻辑视频：日志中 `lsf` 次数减少；「目标已存在」改名仍正确。
- `warm_remote_cache=true` 启动：日志打印预热条数；首条上传前无 `lsf`（若名已在目录中）。

---

## P1-3：rclone 异步化

### 现状

```python
subprocess.run([RCLONE, "moveto", ...])   # 阻塞事件循环
subprocess.run([RCLONE, "lsf", ...])
```

### 方案

统一封装（`tg_to_onedrive.py`）：

```python
async def _rclone_run(*args) -> None:
    proc = await asyncio.create_subprocess_exec(
        RCLONE, *args,
        stdout=asyncio.subprocess.PIPE,
        stderr=asyncio.subprocess.PIPE,
    )
    _, stderr = await proc.communicate()
    if proc.returncode != 0:
        raise RuntimeError(...)

async def rclone_moveto_async(local, name) -> None: ...
async def remote_exists_async(name) -> bool: ...
```

- `transfer()` 改为 `await rclone_moveto_async(...)`
- `resolve_unique_name` 改为 `async`，内部 `await remote_exists_async(...)`

### 预期效果（需有正确预期）

| 维度 | 说明 |
|------|------|
| 单 worker 串行 | **单条视频总耗时几乎不变**（上传仍要传那么久） |
| 事件循环 | 上传等待期间不「堵死」asyncio，Telethon 心跳/事件更顺畅 |
| 后续 | 为 P2「下载与上传流水线」前置条件 |

### 修改文件

- `tg_to_onedrive.py`：rclone 封装、`transfer` / `resolve_unique_name` 改 async

### 验收

- 上传大文件时服务仍可响应（日志时间戳连续、无长时间完全无输出）。
- 功能与改前一致：成功上传、失败报错、moveto 删本地。

---

## 配置汇总（实施后 `config.example.ini` 片段）

```ini
[download]
tmp_dir = tmp
min_interval_sec = 3
max_filesize_mb = 0
connections = 8

# P1-1：并行连接池空闲 N 秒后关闭；0=常驻
sender_pool_idle_sec = 60

# P1-2：启动时是否 lsf 预热远端文件名缓存
warm_remote_cache = false
```

本机 `config.ini` 同步添加上述两项即可；旧配置缺省按代码 fallback（建议默认 `60` / `false`）。

---

## 实施顺序与提交建议

```text
Commit 1  P1-2  remote_exists 内存缓存 + 可选 warm_remote_cache
          （改动小、立刻少 lsf，风险低）

Commit 2  P1-3  rclone 异步封装 + transfer/resolve_unique_name async 化

Commit 3  P1-1  SenderPool + sender_pool_idle_sec + fast_download 接入
          （改动面最大，单独测 bench_dl + 连下两条大文件）
```

每步后：`systemctl restart tg-onedrive`，`journalctl -u tg-onedrive -f` 看一条完整「下载→上传→完成」。

---

## 测试清单

- [ ] 单条大视频（>5MB）：8 连接下载、上传成功、`processed.json` 写入
- [ ] 连续两条大视频：第二条建连次数减少（P1-1）
- [ ] 空闲 1 分钟后一条大视频：能正常下载（池已重建）
- [ ] 故意同名 caption：仍改名，不覆盖（P1-2）
- [ ] `bench_dl.py`（需先 stop 服务）：并行与单连接 hash 一致
- [ ] 重启服务：增量补漏日志正常，无回归

---

## 回滚

| 项 | 回滚方式 |
|----|----------|
| P1-1 | `sender_pool_idle_sec=0` 且代码回退池逻辑，或 revert commit |
| P1-2 | `warm_remote_cache=false`；重启进程即清空内存缓存 |
| P1-3 | revert 为 `subprocess.run` 同步版 |

---

## 风险与对策

| 风险 | 对策 |
|------|------|
| 池内连接僵死 | 首次 `_call` 失败则 `discard` 重建 |
| 缓存与云端不一致 | 默认不预热；未命中仍 `lsf`；进程重启清空 |
| async rclone 子进程僵尸 | `communicate()` 超时可后续加 `asyncio.wait_for` |
| 非主 DC 文件 | 池按 `dc_id` 分桶，与现逻辑一致 |

---

## 预估收益（结合本机历史）

| 场景 | P1-1 | P1-2 | P1-3 |
|------|------|------|------|
| 连发大文件 | 每条省数秒~十余秒建连 | — | — |
| 常「目标已存在」 | — | 每条省 1~9s API | — |
| 单条串行、偶发 | 一般 | 首条后明显 | 几乎无体感 |
| 长期维护 | 中 | 小 | 为 P2 铺路 |

---

*文档版本：与当前 `main` 分支（含增量补漏、`backfill_limit=0`、Telethon WARNING）对齐。*