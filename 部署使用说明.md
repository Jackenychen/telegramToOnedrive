# Telegram → OneDrive 自动转存 · 部署使用说明

把转发到指定 Telegram 群的视频，自动下载并上传到 OneDrive，再由同机的 91 项目扫盘后在站点显示播放。本服务是独立旁路服务，不改动 91 项目。

## 当前部署信息

| 项 | 值 |
|---|---|
| 部署目录 | `/root/telegram` |
| 运行方式 | systemd 服务 `tg-onedrive`（已开机自启） |
| 监听来源 | Telegram 群 `-5217033513` |
| 上传目标 | OneDrive 的 `Telegram/` 目录（与 91 同一账号 `ccwxy1234`） |
| 下载临时目录 | `/root/telegram/tmp/`（上传成功后**自动删除**） |
| 单文件上限 | 4 GB（Premium；超过跳过） |
| 并行下载 | `connections = 8`；连接池空闲 **60s** 后关闭 |
| 处理间隔 | `min_interval_sec = 3` |
| 启动补漏 | **增量**（`id > max(processed)`）；`backfill_limit = 0` |

## 工作流程

```
转发视频到群(-5217033513)
   ↓  tg-onedrive 监听（Telethon）
下载到 tmp/（并行 8 连接 + 连接池复用）
   ↓  rclone moveto（异步子进程，不堵事件循环）
OneDrive:Telegram/（同名检测有内存缓存，减少 lsf）
   ↓  91 扫盘
站点播放（OneDrive 302）
```

## 启动时会不会漏视频？

| 情况 | 行为 |
|------|------|
| 服务一直在线 | 新视频靠实时事件，不丢 |
| 停机一段时间后又启动 | 有 `processed.json` 时自动 **增量补漏**（只拉 `id > max(processed)` 的未处理视频） |
| 删了 `processed.json` | 增量失效；需临时设 `backfill_limit`（如 1000）冷启动扫一遍，扫完改回 `0` |
| `processed` 为空且 `backfill_limit = 0` | 不拉历史，只处理以后新消息 |

日志示例：`增量补漏：拉取 id > 8068 …` → `入队 N 条视频`。

## 怎么用

1. 往群里转发视频；**说明文字**作标题（含 `#` 的行拼接，否则首行，无字则 `TG_日期_消息id`）
2. 相册多条视频：共享 caption，自动 `_01`、`_02` 序号
3. **同名不覆盖**：OneDrive 已有同名则加 `_日期` / `_消息id`；进程内缓存已见过的文件名，少打 `rclone lsf`
4. VPS 自动下载上传 → 91 后台「扫描所有网盘」或等凌晨扫盘

## 运维命令

```bash
journalctl -u tg-onedrive -f
systemctl status  tg-onedrive
systemctl restart tg-onedrive
```

改 `config.ini` 后执行 `systemctl restart tg-onedrive`。

## config.ini 关键项（当前）

| 项 | 当前值 | 说明 |
|---|---|---|
| `source` | `-5217033513` | 监听群 ID；**升级为超级群后 id 会变 -100 开头，需改** |
| `dest_dir` | `Telegram` | OneDrive 目录（须在 91 盘根下） |
| `remote` | `onedrive` | rclone remote 名 |
| `connections` | `8` | Premium 并行连接；`<5MB` 自动单连接 |
| `sender_pool_idle_sec` | `60` | 并行连接池无下载 60s 后关闭；`0` = 一直占着 |
| `warm_remote_cache` | `false` | `true` = 启动时 `lsf` 整目录预热缓存（目录大时启动慢） |
| `min_interval_sec` | `3` | 两条视频处理最小间隔；常 FloodWait 可改 `5` |
| `max_filesize_mb` | `4096` | 超限跳过，`0` = 不限 |
| `backfill_limit` | `0` | 仅 **processed 为空** 时回扫最近 N 条；平时靠增量补漏 |

## 性能相关（P1）

- **连接池**：连下多个大文件时不反复建 8 条 TG 连接；空闲 1 分钟自动断开，不占多少 CPU
- **文件名缓存**：内存记录「云端已有 / 本服务刚上传」的名字，**不落盘**；换机器不拷缓存也行，最多多几次 `lsf`
- **rclone 异步**：上传等待时不阻塞 asyncio，单条串行总时间几乎不变

设计细节见仓库内 `P1-优化方案.md`。

## 重要文件（勿删 / 勿泄露）

| 文件 | 作用 |
|------|------|
| `config.ini` | API 密钥等 |
| `tg_saved.session` | TG 登录态 |
| `processed.json` | 已处理 message id（删了会重复下或需冷启动回扫） |

## 注意事项

- OneDrive 须与 91 **同一账号**，否则扫不到
- rclone token 一般自动刷新；失效时 `rclone config reconnect onedrive:`（`tenant` 留空）
- 群升级超级群要改 `source`
- Premium + `cryptg` + `connections > 1` 才能并行提速；测速：`systemctl stop tg-onedrive` 后 `venv/bin/python bench_dl.py`

## 简单排查

| 现象 | 处理 |
|---|---|
| 停机后漏传 | 确认 `processed.json` 在且服务已重启；看日志「增量补漏」 |
| 视频没上传 | `journalctl -u tg-onedrive -n 50`；`rclone lsd onedrive:` |
| 站点没显示 | OneDrive 有文件后触发 91 扫盘 |
| 下载慢 | 日志 `Premium=True`，`connections`>1，`pip show cryptg` |
| FloodWait 多 | `min_interval_sec` 改 5 或 `connections` 调小 |
| 积压很多 | `venv/bin/python scan_backlog.py` 估 N，临时 `backfill_limit=N` 重启，清完改 `0` |

## 验证

```bash
rclone lsl onedrive:Telegram/
cat /root/telegram/processed.json
```