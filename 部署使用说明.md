# Telegram → OneDrive 自动转存 · 部署使用说明

监听指定 Telegram 群：**视频**上传到 OneDrive `Telegram/`（供 91 扫盘播放）；**音频/语音**上传到网盘根下 **`音频/`**（与视频分开）。独立旁路服务，不改动 91 项目。

## 当前部署信息

| 项 | 值 |
|---|---|
| 部署目录 | `/root/telegram` |
| 运行方式 | systemd `tg-onedrive`（开机自启） |
| 监听来源 | 群 `-5217033513` |
| 视频目录 | `onedrive:Telegram/` |
| 音频目录 | `onedrive:音频/` |
| 临时目录 | `/root/telegram/tmp/`（上传后自动删） |
| 单文件上限 | 4 GB（超限跳过并记入 processed） |
| 并行下载 | `connections = 8`；**≥5MB** 的音视频多连接，更小单连接 |
| 连接池 | 空闲 **60s** 关闭 |
| 处理间隔 | `min_interval_sec = 3` |
| 启动补漏 | 增量 `id > max(processed)`；`backfill_limit = 0` |

## 工作流程

```
转发到群
  ├─ 视频 → tmp/ → onedrive:Telegram/ → 91 扫盘 → 站点播放
  └─ 音频/语音 → tmp/ → onedrive:音频/（不参与 91 视频扫盘，仅网盘存档）
```

## 识别规则

| 类型 | 说明 |
|------|------|
| 视频 | 视频消息或 `video/*` 文档 |
| 音频 | 语音、音频消息或 `audio/*`；与视频互斥（同条不会双传） |
| 相册 | 组内视频进 `Telegram/`，组内音频进 `音频/`，共享 caption + `_01` 序号 |

文件名：caption 中含 `#` 的行拼接；无则首行；无文字则 `TG_日期_消息id` + 扩展名（`.mp3`/`.ogg` 等）。

## 启动补漏

| 情况 | 行为 |
|------|------|
| 正常运行 | 新消息实时入队 |
| 停机后重启 | 增量拉取 `id > max(processed)`，日志：`入队 视频 N 条、音频 M 条` |
| 删 `processed.json` | 临时加大 `backfill_limit` 冷启动 |

## 怎么用

1. **视频**：转发到群 → 等日志 `完成video` → 91 扫盘  
2. **音频**：转发音频文件或语音 → 等日志 `完成audio` → 在 OneDrive **`音频`** 文件夹查看  
3. 同名不覆盖：自动加 `_日期` / `_消息id`

## 运维命令

```bash
journalctl -u tg-onedrive -f
systemctl restart tg-onedrive
rclone lsl onedrive:Telegram/ | tail
rclone lsl onedrive:音频/ | tail
```

## config.ini 关键项

| 项 | 当前值 | 说明 |
|---|---|---|
| `source` | `-5217033513` | 群 ID；升级超级群可能变 `-100` 开头 |
| `dest_dir` | `Telegram` | 视频目标（91 须能扫到） |
| `audio_dest_dir` | `音频` | 音频目标（网盘根下）；**留空=不处理音频** |
| `remote` | `onedrive` | rclone remote |
| `connections` | `8` | ≥5MB 音视频并行；<5MB 单连接 |
| `sender_pool_idle_sec` | `60` | 连接池空闲关闭 |
| `warm_remote_cache` | `false` | 启动预热远端文件名缓存 |
| `min_interval_sec` | `3` | 两条处理间隔 |
| `max_filesize_mb` | `4096` | 超限跳过 |
| `backfill_limit` | `0` | 仅 processed 为空时冷启动回扫 |

## 日志示例

```text
开始监听 source=...，视频→onedrive/Telegram，音频→音频/
下载video id=8100 (120.0MB, 8连接) → xxx.mp4
下载audio id=8140 (84.2MB, 8连接) → xxx.mp3
rclone 上传 → onedrive:音频/xxx.mp3
完成audio id=8140
```

## 重要文件

| 文件 | 说明 |
|------|------|
| `config.ini` | 含密钥，chmod 600 |
| `tg_saved.session` | TG 登录态 |
| `processed.json` | 已处理 message id（音视频共用） |

## 简单排查

| 现象 | 处理 |
|------|------|
| 音频没反应 | `audio_dest_dir` 是否为空；是否真是音频/语音 |
| 大音频很慢 | 确认 ≥5MB 时日志为 `8连接`；Premium + `cryptg` |
| 视频站点没有 | 91 扫盘；`dest_dir` 在 91 盘根下 |
| 停机漏传 | 重启服务看「增量补漏」 |

打包迁机见 **`迁移清单.md`**；性能优化见 **`P1-优化方案.md`**。