# Telegram → OneDrive 自动转存 · 部署使用说明

把转发到指定 Telegram 群的视频，自动下载并上传到 OneDrive，再由同机的 91 项目扫盘后在站点显示播放。本服务是独立旁路服务，不改动 91 项目。

## 当前部署信息

| 项 | 值 |
|---|---|
| 部署目录 | `/root/telegram` |
| 运行方式 | systemd 服务 `tg-onedrive`（已开机自启） |
| 监听来源 | Telegram 群 `-5217033513` |
| 上传目标 | OneDrive 的 `Telegram/` 目录（与 91 同一账号 `ccwxy1234`） |
| 下载临时目录 | `/root/telegram/tmp/`（上传成功后**自动删除**，不留本地副本） |
| 单文件上限 | 4 GB（账号已开 Telegram Premium；超过自动跳过） |
| 下载方式 | 并行 8 连接（Premium 提速，大文件实测约 12 MiB/s，如 470MB 约 40 秒；`<5MB` 走单连接；异常自动回退） |

## 工作流程

```
转发视频到群(-5217033513)
   ↓  tg-onedrive 服务自动监听
下载到 /root/telegram/tmp/
   ↓  rclone moveto 上传（成功后立即删本地）
OneDrive:Telegram/
   ↓  91 扫盘入库
站点显示，点开 OneDrive 302 直连播放
```

## 怎么用

1. 往那个群转发视频（**文字说明**用作标题：取所有含 `#` 标签的行拼接，没有 `#` 行就用第一行，完全无文字用 `TG_日期_消息id`）
   - 一次转发**多条视频+一段文字**（相册）：那段文字会套用到**整组每条视频**，并自动加序号，如 `家庭聚会_01.mp4`、`家庭聚会_02.mp4`，在站点里能看出是同一组
   - **同名不覆盖**：若 OneDrive 上已有同名文件（比如两次都配文字"旅行"），上传前会自动改名补 `_日期`（仍冲突再补 `_消息id`），不会覆盖之前的视频
2. VPS 自动下载 → 上传 OneDrive → 删本地临时文件
3. 到 91 后台点「扫描所有网盘」（或等凌晨自动扫），视频入库，带「Telegram」合集标签

## 视频存哪 / 会不会留本地

- 下载时临时存 `/root/telegram/tmp/`
- `rclone moveto` 上传，**成功后本地文件立即删除**；失败也会删半成品
- 所以 `tmp/` 始终基本为空，不会占盘

## 运维命令

```bash
journalctl -u tg-onedrive -f      # 实时日志（转发时观察）
systemctl status  tg-onedrive     # 状态
systemctl restart tg-onedrive     # 重启
systemctl stop    tg-onedrive     # 停止
systemctl start   tg-onedrive     # 启动
```

## config.ini 关键项

| 项 | 当前值 | 说明 |
|---|---|---|
| `source` | `-5217033513` | 监听的群 ID；**群若升级为超级群，ID 变成 -100 开头，需改这里** |
| `dest_dir` | `Telegram` | OneDrive 目标目录（须在 91 onedrive 根目录下） |
| `remote` | `onedrive` | rclone 的 OneDrive remote 名 |
| `max_filesize_mb` | `4096` | 跳过超过此大小的视频，0=不限（Premium 4GB，免费号 2GB） |
| `connections` | `8` | 并行下载连接数；`1`=单连接。仅 Premium 建议 >1（大文件实测约 12MiB/s、中小文件 5~7，单连接仅 0.5~1）；`<5MB` 自动单连接；撞 FloodWait 就调小 |
| `backfill_limit` | `1000` | 启动时回扫最近 N 条、按 `processed.json` 去重后重新入队（群共约 703 条，1000 全覆盖）；用于补下积压，重启/重启机器后自动续传未完成的。积压清完后可改回 `0` |

> 改完 config.ini 后执行 `systemctl restart tg-onedrive` 生效。

## 重要文件（勿删 / 勿泄露）

- `config.ini`：含 Telegram api_hash，已 chmod 600
- `tg_saved.session`：Telegram 登录态，删了要重新登录
- `processed.json`：已处理消息 id（去重），删了会重复转存
- `.gitignore`：已排除上述敏感文件

## 注意事项

- **OneDrive 账号**：必须和 91 用的同一个（`ccwxy1234`），否则 91 扫不到
- **token 自动续期**：rclone 的 OneDrive token 会自动刷新，正常不用管
  - （历史坑：曾因 rclone 配置里 `tenant=n` 导致刷新失败，已清空修复；若日后再现刷新错误，用 `rclone config reconnect onedrive:`，且 `tenant>` 提示务必留空）
- **群别升级**：普通群成员超 200 / 设公开链接会升级成超级群，ID 变化需更新 `source`
- **下载提速（并行）**：单连接受 Telegram 单管道吞吐限制（约 0.5~1 MiB/s，开不开会员都一样）。本机已开 **Premium** 并设 `connections = 8`，对同一文件多连接并行拉取、带宽叠加（大文件实测约 12 MiB/s，如 470MB 约 40 秒），结果经 sha256 校验与单连接一致，异常自动回退单连接（`<5MB` 小文件直接走单连接）。积压排队致 `file_reference` 过期会自动重取消息换新令牌重试，不退回单连接。已装 `cryptg`（C 实现 AES，避免多连接解密卡 CPU）。免费号别开并行（易 FloodWait）。无需 bot——bot 不能开会员。自助测速：`systemctl stop tg-onedrive` 后跑 `venv/bin/python bench_dl.py`
- **大文件**：账号已开 Premium，单文件上限 4GB（免费账号 2GB）；开了「禁止保存内容」的频道无法下载

## 简单排查

| 现象 | 处理 |
|---|---|
| 视频没传上去 | `journalctl -u tg-onedrive -n 50` 看日志；`rclone lsd onedrive:` 测连接 |
| 站点没显示 | 确认 OneDrive `Telegram/` 有文件；到 91 后台触发扫盘 |
| 服务没运行 | `systemctl status tg-onedrive`，必要时 `restart` |
| 下载很慢 | 确认启动日志有 `Premium=True` 且 `connections`>1；`cryptg` 已装（`venv/bin/pip show cryptg`） |
| token 失效 | `rclone config reconnect onedrive:`（`tenant` 留空） |

## 验证上传是否成功

```bash
rclone lsl onedrive:Telegram/        # 列出已上传的视频
cat /root/telegram/processed.json    # 已成功处理的消息 id
```
